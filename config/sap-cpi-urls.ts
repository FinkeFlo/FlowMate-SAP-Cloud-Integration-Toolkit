/**
 * SAP Cloud Platform Integration Suite URL Patterns
 * 
 * Add your SAP CPI tenant URLs here. The extension will be active on all matching URLs.
 */
export const SAP_CPI_URL_PATTERNS = [
  '*://*.hana.ondemand.com/*',
] as const;
